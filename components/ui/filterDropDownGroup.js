import React from "react";

const FilterDropDownGroup = (props) => {
  const { children } = props;
  return (
    <div className="border-dark border rounded-lg font-slussen">{children}</div>
  );
};

export default FilterDropDownGroup;
